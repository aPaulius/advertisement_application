{% extends 'base.html.twig' %}

{% block content %}

    <h1 class="text-center ">POST ADVERTISEMENT</h1>

    {{ form_start(form)}}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
